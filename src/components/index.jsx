// single selection
// multiple selection

export default function GreatestBasketballPlayer() {

    const [selected, setSelected] = React.useState(null);
    return (
        <div>
            GreatestBasketballPlayer
        </div>
    );
}